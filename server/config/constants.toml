[username]
min_length = 8
max_length = 32
pattern = "^[a-zA-Z0-9_]+$"

[password]
min_length = 8
max_length = 64

[session]
duration = 2592000  # one month, TODO: make str

[signup]
code_duration = 172800  # 2 days